<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Default title fallback (sprečava 500 error) -->
    <title><%= typeof title !== 'undefined' ? title : 'AI Scalper' %> | AI Scalper</title>

    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', sans-serif;
            background: #0f0f23;
            color: #ffffff;
        }
        header {
            background: #11192e;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid #1e2a4a;
        }
        .nav-links a {
            margin-right: 1.5rem;
            text-decoration: none;
            color: #9bbcff;
            font-weight: 500;
        }
        .nav-links a:hover {
            color: #ffffff;
        }
        footer {
            margin-top: 40px;
            padding: 2rem;
            background: #11192e;
            border-top: 1px solid #1e2a4a;
            text-align: center;
            color: #6d89c6;
        }
        .main-container {
            padding: 2rem;
            max-width: 1400px;
            margin: 0 auto;
        }
    </style>
</head>

<body>

<header>
    <div class="logo">🤖 <strong>AI Scalper</strong></div>

    <nav class="nav-links">
        <a href="/">Dashboard</a>
        <a href="/trades">Trades</a>
        <a href="/engine">Engine</a>
        <a href="/settings">Settings</a>
    </nav>

    <div>
        <!-- Default user fallback so login page works -->
        <span style="margin-right: 1rem;">👤 <%= typeof user !== 'undefined' ? user : 'Guest' %></span>
        <a href="/logout" style="color:red; text-decoration:none;">Logout</a>
    </div>
</header>

<div class="main-container">
    <%- body %>
</div>

<footer>
    AI Scalper © <%= new Date().getFullYear() %> &mdash; Engine v1.0
</footer>

</body>
</html>
